<odoo>
    <record id="action_report_orden_venta_pdf" model="ir.actions.report">
        <field name="name">Reporte de Operaciones (Resumen + Detalle)</field>
        <field name="model">bateria.orden.venta</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">bateria_reportes.report_orden_venta_document</field>
        <field name="report_file">bateria_reportes.report_orden_venta_document</field>
        <field name="print_report_name">'Reporte_Ventas_%s' % (object.referencia_pedido if len(object) == 1 else 'Multiple')</field>
        <field name="binding_model_id" ref="model_bateria_orden_venta"/>
        <field name="binding_type">report</field>
    </record>
</odoo>